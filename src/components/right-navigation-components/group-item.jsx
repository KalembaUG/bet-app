import React from 'react';

const GroupItem = ({itemName}) => {
    return (
        <div className='group-item'>
            <span>{itemName}</span>
        </div>
    );
}

export default GroupItem;
